#include"main.h"

char *create_array(unsigned int size, char c);
char *p
	n=10;
{
	if (size == NULL)
		return NULL
}
{
	do (*p = malloc size of char *n)
		while (p == NULL)
}
{
	for (i=0, i<n, i++)
		return (p)
}
