#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _strlen(char *s);{
	char Chaine[]="bonjour";
	int length=strlen(Chaine);
	printf("the lenght of the string = %d",length);
	return 0;}
